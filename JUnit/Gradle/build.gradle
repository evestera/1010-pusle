apply plugin: 'java'
apply plugin: 'application'

mainClassName = "Main"

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.11'
}

test {
    testLogging {
        exceptionFormat = 'full'
        //showStandardStreams = true
    }
}
